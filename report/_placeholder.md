# Thư mục report/

Đặt file báo cáo tiểu luận vào đây khi hoàn thành, ví dụ:

- `Bao_cao_tieu_luan_DeTai43_231A010880.docx` (hoặc `.pdf`)

Yêu cầu: 10-15 trang (không tính phụ lục), đúng cấu trúc mục 5 trong tài
liệu hướng dẫn (Mở đầu, Mục tiêu, Cơ sở lý thuyết, Phương pháp thực hiện,
Kết quả, Đánh giá bảo mật, Kết luận, Tài liệu tham khảo).

*(File này (`_placeholder.md`) chỉ để giữ chỗ thư mục trên Git — có thể
xóa sau khi đã thêm báo cáo thật vào đây.)*
